const customError = require('./customError');
const ctrlWrapper = require('./ctrlWrapper');
const handleMongooseError = require('./handleMongooseError');

module.exports = { customError, ctrlWrapper, handleMongooseError };
